import { FilterProductionProdPipe } from './filter-production-prod.pipe';

describe('FilterProductionProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductionProdPipe();
    expect(pipe).toBeTruthy();
  });
});

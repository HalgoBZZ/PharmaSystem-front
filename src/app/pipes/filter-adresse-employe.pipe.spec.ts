import { FilterAdresseEmployePipe } from './filter-adresse-employe.pipe';

describe('FilterAdresseEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdresseEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

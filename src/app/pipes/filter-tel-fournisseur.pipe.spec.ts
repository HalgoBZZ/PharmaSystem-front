import { FilterTelFournisseurPipe } from './filter-tel-fournisseur.pipe';

describe('FilterTelFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTelFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterNomEmployePipe } from './filter-nom-employe.pipe';

describe('FilterNomEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

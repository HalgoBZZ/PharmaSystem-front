import { FilterNomFournisseurPipe } from './filter-nom-fournisseur.pipe';

describe('FilterNomFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterModificationCongePipe } from './filter-modification-conge.pipe';

describe('FilterModificationCongePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModificationCongePipe();
    expect(pipe).toBeTruthy();
  });
});

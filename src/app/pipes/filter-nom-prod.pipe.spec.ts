import { FilterNomProdPipe } from './filter-nom-prod.pipe';

describe('FilterNomProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomProdPipe();
    expect(pipe).toBeTruthy();
  });
});

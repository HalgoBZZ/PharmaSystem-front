import { FilterCauseCongePipe } from './filter-cause-conge.pipe';

describe('FilterCauseCongePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCauseCongePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterQuantiteProdPipe } from './filter-quantite-prod.pipe';

describe('FilterQuantiteProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuantiteProdPipe();
    expect(pipe).toBeTruthy();
  });
});

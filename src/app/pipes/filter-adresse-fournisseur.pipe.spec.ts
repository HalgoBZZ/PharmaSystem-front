import { FilterAdresseFournisseurPipe } from './filter-adresse-fournisseur.pipe';

describe('FilterAdresseFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdresseFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

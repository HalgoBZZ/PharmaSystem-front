import { FilterModificationCategoriePipe } from './filter-modification-categorie.pipe';

describe('FilterModificationCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModificationCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});

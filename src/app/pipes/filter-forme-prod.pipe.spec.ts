import { FilterFormeProdPipe } from './filter-forme-prod.pipe';

describe('FilterFormeProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFormeProdPipe();
    expect(pipe).toBeTruthy();
  });
});

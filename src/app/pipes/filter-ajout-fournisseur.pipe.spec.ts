import { FilterAjoutFournisseurPipe } from './filter-ajout-fournisseur.pipe';

describe('FilterAjoutFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAjoutFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

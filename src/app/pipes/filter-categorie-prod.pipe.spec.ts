import { FilterCategorieProdPipe } from './filter-categorie-prod.pipe';

describe('FilterCategorieProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCategorieProdPipe();
    expect(pipe).toBeTruthy();
  });
});

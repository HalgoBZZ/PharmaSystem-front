import { FilterReferenceProdPipe } from './filter-reference-prod.pipe';

describe('FilterReferenceProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterReferenceProdPipe();
    expect(pipe).toBeTruthy();
  });
});

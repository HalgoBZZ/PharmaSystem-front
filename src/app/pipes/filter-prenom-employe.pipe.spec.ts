import { FilterPrenomEmployePipe } from './filter-prenom-employe.pipe';

describe('FilterPrenomEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrenomEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

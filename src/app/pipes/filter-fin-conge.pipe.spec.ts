import { FilterFinCongePipe } from './filter-fin-conge.pipe';

describe('FilterFinCongePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFinCongePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterTelEmployePipe } from './filter-tel-employe.pipe';

describe('FilterTelEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTelEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

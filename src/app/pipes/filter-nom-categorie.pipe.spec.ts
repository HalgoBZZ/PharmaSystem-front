import { FilterNomCategoriePipe } from './filter-nom-categorie.pipe';

describe('FilterNomCategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNomCategoriePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterSexeEmployePipe } from './filter-sexe-employe.pipe';

describe('FilterSexeEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSexeEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

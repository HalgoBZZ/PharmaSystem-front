import { FilterEmailFournisseurPipe } from './filter-email-fournisseur.pipe';

describe('FilterEmailFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmailFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterRoleEmployePipe } from './filter-role-employe.pipe';

describe('FilterRoleEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoleEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

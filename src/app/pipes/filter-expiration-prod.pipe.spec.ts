import { FilterExpirationProdPipe } from './filter-expiration-prod.pipe';

describe('FilterExpirationProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExpirationProdPipe();
    expect(pipe).toBeTruthy();
  });
});

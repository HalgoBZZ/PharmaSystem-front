import { FilterPrixProdPipe } from './filter-prix-prod.pipe';

describe('FilterPrixProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrixProdPipe();
    expect(pipe).toBeTruthy();
  });
});

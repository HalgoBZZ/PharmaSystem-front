import { FilterAjoutCongePipe } from './filter-ajout-conge.pipe';

describe('FilterAjoutCongePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAjoutCongePipe();
    expect(pipe).toBeTruthy();
  });
});

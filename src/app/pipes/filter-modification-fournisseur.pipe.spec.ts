import { FilterModificationFournisseurPipe } from './filter-modification-fournisseur.pipe';

describe('FilterModificationFournisseurPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModificationFournisseurPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterDateNaissEmployePipe } from './filter-date-naiss-employe.pipe';

describe('FilterDateNaissEmployePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDateNaissEmployePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterFournisseurProdPipe } from './filter-fournisseur-prod.pipe';

describe('FilterFournisseurProdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFournisseurProdPipe();
    expect(pipe).toBeTruthy();
  });
});

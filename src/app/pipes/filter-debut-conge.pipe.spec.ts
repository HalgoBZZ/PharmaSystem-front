import { FilterDebutCongePipe } from './filter-debut-conge.pipe';

describe('FilterDebutCongePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDebutCongePipe();
    expect(pipe).toBeTruthy();
  });
});
